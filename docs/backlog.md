# 待办事项

本文件记录已确认但尚未执行的工作，以及中断时的准确状态。条目完成后应从此处移除，其内容进入正式文档。

> 本文件为内部工作记录，暂只有中文。

## 当前状态（2026-09-13 夜）

- **11 个提交在本地，未推送** ✓（换行符已统一为 LF，并新增 `.gitattributes` 声明约定）。推送被 GitHub 拒收：`gh` 令牌缺 `workflow` 权限，而批次里含 `.github/workflows/test.yml`。解除方式：`gh auth refresh -h github.com -s workflow`，之后推送并把 `v0.1.0`（当前指向旧提交 `5f8af0a`）移到正确提交。
- 312 项单元测试全绿，插件自检通过，工作区干净。
- 批次内部的提交历史里仍留有换行符造成的整文件 diff（端点已干净）。若希望公开历史也干净，可在推送前把这批压缩成按功能划分的少数几个提交。
- 已在真机验证：表格渲染（`· 项：余额 · 值：¥20.23`）、重复消息去重、`ask_user_question` 按钮化提问。
- **待真机验证**：markdown 渲染（上一次 A/B 因 bug 无效，需重启后重测）、`/find` 的实际搜索、`/doctor` 的首次实跑。
- 已知未修的 bug：无。

### 下次继续时

1. 问用户是否已授权 `workflow`，是则推送整批并修正 Release 指向。
2. 让用户发 `/restart`，然后请求「再发渲染测试」——A 这次会真的走 markdown 路径，一次即可定论；若 A 露出 `**` 则把 `markdownMode` 设为 `never`。
3. 若用户想继续拓展功能，候选见下（审批记忆暂缓：DSH 的审批请求只带 `toolName` 与 `callId`，参数需回查 `assistant/message` 的 tool-call 块，可实现但涉及安全取舍）。

## 待推送批次：GitHub 系统性更新
## 待推送批次：GitHub 系统性更新

以下工作已确认，将在下一次推送时一并完成。

| # | 项目 | 说明 | 状态 |
|---|---|---|---|
| 1 | 重复消息去重 | 平台可能重复推送相同的 `msg_id`，此前无防护，会导致同一条指令被执行两次。现已按会话记录最近 50 个已处理 id 并丢弃重复项，判断位于准入之后、分发之前 | **已完成，待推送** |
| 2 | GitHub Actions CI | 推送时自动执行 `npm test`（300 项）。公开仓库无需额外密钥 | 待做 |
| 3 | README 徽章 | CI 状态、MIT 许可、Node 版本 | 待做 |
| 4 | `v0.1.0` tag 与 Release | 附正式发布说明，内容由现有提交汇总 | 待做 |
| 5 | 文档收尾 | README「开发」一节补充 CI 说明；中英文一致 | 待做 |

### 待决定

| # | 项目 | 选项 |
|---|---|---|
| 6 | 套餐窗口告警 | 5 小时 / 本周 / 本月额度接近上限时提醒一次，与余额告警同理。数据现成，成本小 |
| 7 | 代码注释语言 | 注释目前为中文，是公开仓库最大的语言缺口。可选：(a) 维持现状并在 README 说明；(b) 只翻核心模块；(c) 全量英文化 |
| 8 | CI 运行平台 | (a) 仅 windows，完全贴合真实环境；(b) ubuntu + windows 双矩阵，覆盖更好，但 ubuntu 未在本地验证 |

## 功能拓展候选

按"减少手机端操作"的价值排序。

| 项目 | 价值 | 可行性 |
|---|---|---|
| 审批记忆 | 同一回合内针对完全相同的审批请求（同工具、同参数）记住上一次决定，减少反复点击 | 桥接侧可实现；DSH 的审批粒度只有 `allowed-once` 与 `rejected`，因此这属于替用户做安全决定，须显式开启且范围受限 |
| 长回答自动转文件 | 超过 N 条时改为「首段 + 完整文件」，减少消息刷屏 | 桥接侧可实现，成本小 |
| ~~会话搜索（`/find`）~~ | **已完成**：读取 DSH 的 `sessionController.search`，命中项直接成为 `/resume` 列表；已归档会话不会经搜索重新出现；未挂载 sessionQuery 时明确报错而非显示"无结果" | — |
| 截图窗口菜单 | `/screen` 列出可截取的窗口编号，`/screen <编号>` 直接截取，免去输入进程名 | 需扩展 `tools/capture-window.ps1` 增加列举模式 |
| ~~`/log` 与 `/doctor`~~ | **已完成**：`/log` 在手机端读取本插件日志尾部（仅 owner）；`/doctor` 主动执行检查并给出结论，而非仅罗列状态 | — |
| 待办提醒可见 | `/status` 中显示当前会话的待办提醒（读取会话中的 `schedule/change` 事件） | DSH 的 schedule 未对外提供服务，仅有 agent 工具，因此只能读取、不能由命令创建 |
